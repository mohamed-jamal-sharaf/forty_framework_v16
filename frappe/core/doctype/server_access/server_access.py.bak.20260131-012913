# -*- coding: utf-8 -*-
"""
Server Access: فتح/تعديل ملفات الدوكتايب مباشرة من داخل Frappe
يسمح بقراءة/حفظ ملفات: .py .js .json .md .html .css داخل مجلد doctype الفعلي.
- يحمي من الخروج خارج المجلد (لا يسمح بـ .. أو /)
- يعمل نسخة احتياطية تلقائيًا قبل كل حفظ في مجلد backups منظم حسب نوع الملف
- يحتفظ بجميع النسخ الاحتياطية بدون حذف
"""

import os
import shutil
import time
import frappe
from frappe.model.document import Document

# الامتدادات المسموح تعديلها
ALLOWED_EXT = {".py", ".js", ".json", ".md", ".html", ".css"}

# مجلد النسخ الاحتياطية الرئيسي
BACKUP_FOLDER_NAME = "backups"


# -----------------------
# DocType class
# -----------------------
class ServerAccess(Document):
    # begin: auto-generated types
    # This code is auto-generated. Do not modify anything in this block.

    from typing import TYPE_CHECKING

    if TYPE_CHECKING:
        from frappe.types import DF

        file_content: DF.Code | None
        filename: DF.Literal[None]
        last_op_log: DF.SmallText | None
        target_doctype: DF.Link | None
    # end: auto-generated types

    pass


# -----------------------
# Helper: Check filename safety
# -----------------------
def _is_safe_filename(name: str) -> bool:
    """تأكد أن الاسم آمن (بدون / أو \\ أو ..)."""
    return bool(name) and ("/" not in name) and ("\\" not in name) and (".." not in name)


# -----------------------
# Helper: Get file extension folder name
# -----------------------
def _get_extension_folder(filename: str) -> str:
    """
    إرجاع اسم المجلد حسب امتداد الملف
    مثال: file.py → py, file.js → js
    """
    _, ext = os.path.splitext(filename)
    # إزالة النقطة من الامتداد
    return ext.lower().lstrip(".")


# -----------------------
# Helper: Get doctype folder path
# -----------------------
def _doctype_folder_path(doctype_name: str) -> str:
    """
    يرجّع المسار الفعلي لمجلد الدوكتايب:
    apps/<app>/<app>/<module>/doctype/<scrubbed_doctype>
    """
    if not doctype_name:
        frappe.throw("Target DocType is required.")

    try:
        doctype = frappe.get_doc("DocType", doctype_name)
    except Exception:
        frappe.throw(f"Invalid DocType: {doctype_name}")

    module = doctype.module
    module_path = frappe.get_module_path(module)
    folder = frappe.scrub(doctype_name)
    path = os.path.join(module_path, "doctype", folder)

    if not os.path.isdir(path):
        frappe.throw(f"Doctype folder not found: {path}")

    return path


# -----------------------
# Helper: Get or create backup folder for specific extension
# -----------------------
def _get_backup_folder(base_path: str, filename: str) -> str:
    """
    إنشاء مجلد backups/<extension> داخل مجلد الدوكتايب إذا لم يكن موجوداً
    مثال: backups/py/, backups/js/, backups/json/
    """
    ext_folder = _get_extension_folder(filename)
    backup_folder = os.path.join(base_path, BACKUP_FOLDER_NAME, ext_folder)
    
    if not os.path.exists(backup_folder):
        os.makedirs(backup_folder)
    
    return backup_folder


# -----------------------
# API: list files
# -----------------------
@frappe.whitelist()
def list_doctype_files(target_doctype: str) -> list:
    """إرجاع قائمة الملفات المسموح تعديلها داخل مجلد الدوكتايب."""
    base = _doctype_folder_path(target_doctype)
    files = []
    for fn in sorted(os.listdir(base)):
        # تجاهل مجلد النسخ الاحتياطية
        if fn == BACKUP_FOLDER_NAME:
            continue
        full = os.path.join(base, fn)
        if os.path.isfile(full):
            _, ext = os.path.splitext(fn)
            if ext.lower() in ALLOWED_EXT:
                files.append(fn)
    return files


# -----------------------
# API: read file
# -----------------------
@frappe.whitelist()
def read_doctype_file(target_doctype: str, filename: str) -> str:
    """قراءة محتوى ملف داخل مجلد الدوكتايب."""
    if not _is_safe_filename(filename):
        frappe.throw("Invalid file name.")

    base = _doctype_folder_path(target_doctype)
    full = os.path.join(base, filename)

    if not os.path.isfile(full):
        frappe.throw("File not found.")

    with open(full, "r", encoding="utf-8") as f:
        return f.read()


# -----------------------
# API: Get non-frappe DocTypes
# -----------------------
@frappe.whitelist()
def get_non_frappe_doctypes(doctype, txt, searchfield, start, page_len, filters):
    """Get DocTypes that don't belong to the frappe app"""
    
    # Get all modules from frappe app
    frappe_modules = frappe.get_all(
        "Module Def",
        filters={"app_name": "frappe"},
        pluck="name"
    )
    
    # Return DocTypes not in frappe modules
    return frappe.db.sql("""
        SELECT name, module
        FROM `tabDocType`
        WHERE 
            module NOT IN %(frappe_modules)s
            AND (name LIKE %(txt)s OR module LIKE %(txt)s)
            AND istable = 0
        ORDER BY name
        LIMIT %(start)s, %(page_len)s
    """, {
        "frappe_modules": frappe_modules,
        "txt": f"%{txt}%",
        "start": start,
        "page_len": page_len
    })


# -----------------------
# API: save file with backup (organized by extension)
# -----------------------
@frappe.whitelist()
def save_doctype_file(target_doctype: str, filename: str, content: str) -> str:
    """
    حفظ المحتوى في الملف المحدد مع إنشاء Backup تلقائي في مجلد منظم:
    backups/<extension>/<filename>.bak.YYYYmmdd-HHMMSS
    يحتفظ بجميع النسخ الاحتياطية بدون حذف.
    """
    if not _is_safe_filename(filename):
        frappe.throw("Invalid file name.")

    base = _doctype_folder_path(target_doctype)
    full = os.path.join(base, filename)

    if not os.path.isfile(full):
        frappe.throw("File not found.")

    # الحصول على/إنشاء مجلد النسخ الاحتياطية حسب الامتداد
    ext_folder = _get_extension_folder(filename)
    backup_folder = _get_backup_folder(base, filename)

    # إنشاء نسخة احتياطية في مجلد backups/<extension>
    ts = time.strftime("%Y%m%d-%H%M%S")
    backup_filename = f"{filename}.bak.{ts}"
    backup_path = os.path.join(backup_folder, backup_filename)

    try:
        shutil.copy2(full, backup_path)
    except Exception as e:
        frappe.throw(f"Failed to backup file: {e}")

    # الكتابة في الملف
    try:
        with open(full, "w", encoding="utf-8", newline="\n") as f:
            f.write(content if content is not None else "")
    except Exception as e:
        frappe.throw(f"Failed to write file: {e}")

    return f"✅ Saved. Backup: {BACKUP_FOLDER_NAME}/{ext_folder}/{backup_filename}"


# -----------------------
# API: list all backup folders
# -----------------------
@frappe.whitelist()
def list_backup_folders(target_doctype: str) -> list:
    """إرجاع قائمة مجلدات النسخ الاحتياطية (حسب الامتداد)."""
    base = _doctype_folder_path(target_doctype)
    backup_base = os.path.join(base, BACKUP_FOLDER_NAME)
    
    if not os.path.exists(backup_base):
        return []
    
    folders = []
    for fn in sorted(os.listdir(backup_base)):
        full_path = os.path.join(backup_base, fn)
        if os.path.isdir(full_path):
            # حساب عدد الملفات في كل مجلد
            file_count = len([f for f in os.listdir(full_path) if os.path.isfile(os.path.join(full_path, f))])
            folders.append({
                "extension": fn,
                "path": f"{BACKUP_FOLDER_NAME}/{fn}",
                "file_count": file_count
            })
    
    return folders


# -----------------------
# API: list backups for a file
# -----------------------
@frappe.whitelist()
def list_file_backups(target_doctype: str, filename: str) -> list:
    """إرجاع قائمة النسخ الاحتياطية لملف معين."""
    if not _is_safe_filename(filename):
        frappe.throw("Invalid file name.")

    base = _doctype_folder_path(target_doctype)
    ext_folder = _get_extension_folder(filename)
    backup_folder = os.path.join(base, BACKUP_FOLDER_NAME, ext_folder)
    
    if not os.path.exists(backup_folder):
        return []
    
    backups = []
    prefix = f"{filename}.bak."
    
    for fn in sorted(os.listdir(backup_folder), reverse=True):
        if fn.startswith(prefix):
            full_path = os.path.join(backup_folder, fn)
            stat = os.stat(full_path)
            backups.append({
                "filename": fn,
                "timestamp": fn.replace(prefix, ""),
                "size": stat.st_size,
                "size_formatted": _format_file_size(stat.st_size),
                "created": time.ctime(stat.st_mtime),
                "folder": ext_folder
            })
    
    return backups


# -----------------------
# API: restore backup
# -----------------------
@frappe.whitelist()
def restore_backup(target_doctype: str, filename: str, backup_filename: str) -> str:
    """استعادة نسخة احتياطية معينة."""
    if not _is_safe_filename(filename) or not _is_safe_filename(backup_filename):
        frappe.throw("Invalid file name.")

    base = _doctype_folder_path(target_doctype)
    ext_folder = _get_extension_folder(filename)
    backup_folder = os.path.join(base, BACKUP_FOLDER_NAME, ext_folder)
    
    original_file = os.path.join(base, filename)
    backup_file = os.path.join(backup_folder, backup_filename)
    
    if not os.path.isfile(backup_file):
        frappe.throw("Backup file not found.")
    
    # إنشاء نسخة احتياطية من الملف الحالي قبل الاستعادة
    ts = time.strftime("%Y%m%d-%H%M%S")
    pre_restore_backup = os.path.join(backup_folder, f"{filename}.pre-restore.{ts}")
    
    try:
        shutil.copy2(original_file, pre_restore_backup)
        shutil.copy2(backup_file, original_file)
    except Exception as e:
        frappe.throw(f"Failed to restore backup: {e}")
    
    return f"✅ Restored from: {ext_folder}/{backup_filename}"


# -----------------------
# API: read backup content
# -----------------------
@frappe.whitelist()
def read_backup_file(target_doctype: str, filename: str, backup_filename: str) -> str:
    """قراءة محتوى نسخة احتياطية معينة."""
    if not _is_safe_filename(filename) or not _is_safe_filename(backup_filename):
        frappe.throw("Invalid file name.")

    base = _doctype_folder_path(target_doctype)
    ext_folder = _get_extension_folder(filename)
    backup_folder = os.path.join(base, BACKUP_FOLDER_NAME, ext_folder)
    
    backup_file = os.path.join(backup_folder, backup_filename)
    
    if not os.path.isfile(backup_file):
        frappe.throw("Backup file not found.")
    
    with open(backup_file, "r", encoding="utf-8") as f:
        return f.read()


# -----------------------
# API: delete specific backup
# -----------------------
@frappe.whitelist()
def delete_backup(target_doctype: str, filename: str, backup_filename: str) -> str:
    """حذف نسخة احتياطية معينة."""
    if not _is_safe_filename(filename) or not _is_safe_filename(backup_filename):
        frappe.throw("Invalid file name.")

    base = _doctype_folder_path(target_doctype)
    ext_folder = _get_extension_folder(filename)
    backup_folder = os.path.join(base, BACKUP_FOLDER_NAME, ext_folder)
    
    backup_file = os.path.join(backup_folder, backup_filename)
    
    if not os.path.isfile(backup_file):
        frappe.throw("Backup file not found.")
    
    try:
        os.remove(backup_file)
    except Exception as e:
        frappe.throw(f"Failed to delete backup: {e}")
    
    return f"🗑️ Deleted: {ext_folder}/{backup_filename}"


# -----------------------
# Helper: Format file size
# -----------------------
def _format_file_size(size_bytes: int) -> str:
    """تنسيق حجم الملف بشكل قابل للقراءة."""
    if size_bytes == 0:
        return "0 B"
    
    units = ["B", "KB", "MB", "GB"]
    unit_index = 0
    size = float(size_bytes)
    
    while size >= 1024 and unit_index < len(units) - 1:
        size /= 1024
        unit_index += 1
    
    return f"{size:.1f} {units[unit_index]}"
```

**New Folder Structure:**
```
doctype/
├── my_doctype/
│   ├── my_doctype.py
│   ├── my_doctype.js
│   ├── my_doctype.json
│   ├── my_doctype.css
│   └── backups/                        ← Main backup folder
│       ├── py/                         ← Python backups
│       │   ├── my_doctype.py.bak.20250131-143022
│       │   ├── my_doctype.py.bak.20250131-150015
│       │   └── my_doctype.py.pre-restore.20250131-160000
│       ├── js/                         ← JavaScript backups
│       │   ├── my_doctype.js.bak.20250131-144500
│       │   └── my_doctype.js.bak.20250131-151230
│       ├── json/                       ← JSON backups
│       │   └── my_doctype.json.bak.20250131-145000
│       ├── css/                        ← CSS backups
│       │   └── my_doctype.css.bak.20250131-152000
│       ├── html/                       ← HTML backups
│       └── md/                         ← Markdown backups